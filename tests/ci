#!/usr/bin/env bash
set -euo pipefail

bash -n /repo/src/install.sh /repo/src/yams
shopt -s globstar nullglob
tests=(/repo/tests/**/*.bats)
((${#tests[@]} > 0))
exec bats --formatter tap --print-output-on-failure "${tests[@]}"
